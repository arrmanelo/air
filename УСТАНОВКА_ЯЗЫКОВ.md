# 🌍 Установка мультиязычности

## ✅ Что добавлено:

1. **Три языка:** Русский (основной), English, Қазақша
2. **Header компонент** с иконкой профиля и меню языков
3. **Настоящая иконка Google** (SVG, как на других сайтах)
4. **Профиль в хедере** - не выходит при переходе на главную

---

## 🔧 Что нужно сделать:

### 1. Подтяни изменения
```bash
git pull
```

### 2. Установи i18n пакет
```bash
cd frontend
npm install @nuxtjs/i18n
cd ..
```

### 3. Пересобери Docker
```bash
docker-compose up -d --build frontend
```

Или перезапусти:
```bash
docker-compose restart frontend
```

### 4. Подожди 1-2 минуты

### 5. Открой сайт
http://localhost:3000

---

## 🎯 Что теперь есть:

### Header с профилем
```
┌────────────────────────────────────────┐
│ 🌍 EcoMonitor    [🇷🇺 RU▼]  [👤 Имя▼] │
└────────────────────────────────────────┘
```

При клике на флаг - выбор языка:
```
┌───────────────┐
│ 🇷🇺 Русский   │ ✓
│ 🇬🇧 English   │
│ 🇰🇿 Қазақша   │
└───────────────┘
```

При клике на профиль:
```
┌──────────────────┐
│ Твое Имя         │
│ email@gmail.com  │
├──────────────────┤
│ 📊 Dashboard     │
│ 🚪 Выйти        │
└──────────────────┘
```

### Кнопка Google с иконкой
Теперь кнопка выглядит профессионально:
```
┌──────────────────────────────┐
│  [G] Continue with Google    │
└──────────────────────────────┘
```

Где `[G]` - настоящая цветная иконка Google (SVG).

---

## 🌐 Как работают языки:

### Переключение
1. Нажми на флаг в хедере
2. Выбери язык
3. Весь сайт переключится

### Язык сохраняется
- В cookies (i18n_locale)
- При перезагрузке страницы язык остается

### Что переводится:
- ✅ Навигация (Главная, Вход, Выход)
- ✅ Страница логина (все тексты)
- ✅ AI Chat (приветствия, подсказки)
- ✅ Кнопки и ссылки

---

## 📁 Файлы переводов:

`frontend/locales/ru.json` - Русский
`frontend/locales/en.json` - English
`frontend/locales/kz.json` - Қазақша

### Как добавить перевод:

1. Открой файл языка (например `ru.json`)
2. Добавь ключ и перевод:
```json
{
  "myKey": "Мой текст"
}
```
3. Используй в коде:
```vue
<template>
  <div>{{ $t('myKey') }}</div>
</template>
```

---

## 🎨 Header на всех страницах:

Header автоматически появляется на:
- ✅ Главной странице (/)
- ✅ Dashboard (/dashboard)
- ✅ Всех других страницах

Если залогинен - показывает профиль
Если нет - показывает кнопку "Войти"

---

## 🔍 Проверка:

### 1. Открой сайт
```
http://localhost:3000
```

### 2. Проверь Header
Должен быть вверху страницы с:
- Логотипом 🌍 EcoMonitor
- Переключателем языка 🇷🇺
- Кнопкой "Войти" (или профилем если залогинен)

### 3. Переключи язык
Нажми на флаг → выбери English → весь текст на английском!

### 4. Войди через Google
1. Нажми "Войти"
2. Увидишь кнопку с настоящей иконкой Google
3. Нажми "Continue with Google"
4. После входа - в хедере твой профиль!

### 5. Проверь что не выходит
После входа:
- Нажми на логотип → вернешься на главную
- **Профиль остается в хедере!** ✅
- Не выходит из аккаунта! ✅

---

## 🐛 Если что-то не работает:

### Ошибка "Cannot find module @nuxtjs/i18n"
```bash
cd frontend
npm install @nuxtjs/i18n
cd ..
docker-compose restart frontend
```

### Header не появился
Проверь что `app.vue` содержит:
```vue
<template>
  <div id="app">
    <Header />  ← Должно быть!
    <NuxtPage />
    <ChatAssistant />
  </div>
</template>
```

### Языки не переключаются
1. Очисти кэш браузера (Ctrl+Shift+Del)
2. Жесткая перезагрузка (Ctrl+Shift+R)
3. Проверь что файлы `locales/*.json` существуют

---

## 📸 Как должно выглядеть:

### Страница логина:
```
┌─────────────────────────────────┐
│     🌍 EcoMonitor               │
│                                 │
│  Welcome back                   │
│  Sign in to access...           │
│                                 │
│  Email: [____________]          │
│  Password: [____________]       │
│                                 │
│  [    Sign In →    ]            │
│                                 │
│  Or continue with               │
│                                 │
│  [[G] Continue with Google]     │ ← Настоящая иконка!
│                                 │
└─────────────────────────────────┘
```

### Header (залогинен):
```
┌────────────────────────────────────────────────┐
│ 🌍 EcoMonitor        🇷🇺 RU ▼    👤 Имя ▼     │
└────────────────────────────────────────────────┘
```

### Header (не залогинен):
```
┌────────────────────────────────────────────────┐
│ 🌍 EcoMonitor        🇷🇺 RU ▼    [Войти]      │
└────────────────────────────────────────────────┘
```

---

## ✅ Итого:

После установки будет:
1. ✅ Переключатель языков (RU/EN/KZ)
2. ✅ Header с профилем (не выходит!)
3. ✅ Настоящая иконка Google
4. ✅ Сохранение выбранного языка
5. ✅ Все переведено на 3 языка

**Команды по порядку:**
```bash
git pull
cd frontend && npm install @nuxtjs/i18n && cd ..
docker-compose restart frontend
```

Открой http://localhost:3000 и наслаждайся! 🎉
