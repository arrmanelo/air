# 🔧 РЕШЕНИЕ ТВОИХ ПРОБЛЕМ

## Проблема 1: Ошибки в VS Code (TypeScript)

```
Cannot find name 'defineNuxtConfig'
Cannot find name 'process'
```

### Решение:
```bash
cd frontend
npm install
```

Это установит все зависимости локально и ошибки исчезнут.

---

## Проблема 2: "Нужно ли качать PostgreSQL?"

### Ответ: НЕТ!

PostgreSQL уже в `docker-compose.yml` и запустится автоматически.

**Ничего не качайте!** Просто запустите:
```bash
docker-compose up -d
```

PostgreSQL запустится в контейнере `eco_postgres`.

### Как посмотреть данные (без установки pgAdmin):

```bash
# Войти в PostgreSQL
docker exec -it eco_postgres psql -U eco_monitor -d environmental_monitoring

# Посмотреть пользователей
SELECT * FROM users;

# Выйти
\q
```

---

## Проблема 3: "Firebase нужен?"

### Ответ: НЕТ! Firebase больше НЕ используется!

Мы **убрали** Firebase и заменили на:
- ✅ Google OAuth (напрямую)
- ✅ PostgreSQL (вместо Firestore)
- ✅ JWT токены

**Ничего в Firebase регистрировать не нужно!**

Нужно только:
1. Зарегистрироваться в **Google Cloud Console** (для OAuth)
2. Получить Client ID и Client Secret
3. Добавить в `.env` файл

---

## Проблема 4: "localhost:3000 старый фронт, localhost:3001 без бэка"

### Что происходит:

**localhost:3001** (npm run dev):
- Это локальная разработка фронтенда
- Запускается командой `npm run dev`
- **Бэкенд НЕ запускается** (только фронт)

**localhost:3000** (Docker):
- Это полное приложение (фронт + бэк)
- Запускается командой `docker-compose up -d`
- **Все работает вместе**

### Решение:

#### Вариант 1: Используй Docker (все вместе) ✅ РЕКОМЕНДУЕТСЯ

```bash
# Останови все старое
docker-compose down

# Пересобери и запусти
docker-compose up -d --build

# Жди 30 секунд пока все запустится

# Открой браузер
# http://localhost:3000
```

Если показывает старый фронт:
```bash
# Жесткая перезагрузка в браузере
Ctrl + Shift + R  (Windows)
Cmd + Shift + R   (Mac)
```

#### Вариант 2: Разработка (фронт локально, бэк в Docker)

```bash
# 1. Запусти только бэкенды в Docker
docker-compose up -d postgres auth_service iot_service analytics_service alert_service

# 2. В другом терминале запусти фронт
cd frontend
npm run dev

# Откроется на http://localhost:3001
```

---

## Проблема 5: Какой стек у нас?

### ТЕКУЩИЙ СТЕК (что РЕАЛЬНО используется):

```
Frontend:
  ✅ Nuxt 3
  ✅ Vue 3
  ✅ Tailwind CSS

Backend:
  ✅ FastAPI (Python)
  ✅ Auth Service (Google OAuth + JWT)
  ✅ IoT Service (данные сенсоров)
  ✅ Analytics Service (AI анализ)
  ✅ Alert Service (оповещения)

Database:
  ✅ PostgreSQL (через Docker!)
  ✅ TimescaleDB (для временных рядов)

Authentication:
  ✅ Google OAuth 2.0 (напрямую, БЕЗ Firebase!)
  ✅ JWT токены

Infrastructure:
  ✅ Docker
  ✅ Docker Compose
```

### ❌ Что НЕ используется:

- ❌ Firebase (убрали!)
- ❌ MySQL
- ❌ MongoDB
- ❌ Redis (пока нет)

---

## 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ (с нуля):

### Шаг 1: Установи зависимости фронтенда
```bash
cd frontend
npm install
cd ..
```

### Шаг 2: Получи Google OAuth credentials

1. Иди на https://console.cloud.google.com/
2. Создай проект
3. APIs & Services → Credentials → Create Credentials → OAuth client ID
4. Web application
5. Authorized redirect URIs: `http://localhost:8004/auth/google/callback`
6. Скопируй Client ID и Client Secret

### Шаг 3: Создай .env файл
```bash
# Скопируй пример
cp .env.example .env

# Открой и вставь свои данные
nano .env
```

```bash
GOOGLE_CLIENT_ID=твой-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=твой-client-secret
JWT_SECRET_KEY=любая-строка-32-символа-минимум
```

### Шаг 4: Запусти Docker
```bash
docker-compose up -d --build
```

### Шаг 5: Дождись запуска (30-60 секунд)
```bash
# Проверь что все запустилось
docker-compose ps

# Должно быть:
# eco_postgres - Up
# eco_auth_service - Up
# eco_iot_service - Up
# eco_analytics_service - Up
# eco_alert_service - Up
# eco_frontend - Up
```

### Шаг 6: Открой браузер
http://localhost:3000

### Шаг 7: Войди через Google
1. Нажми "Login"
2. Нажми "Continue with Google"
3. Выбери свой Google аккаунт
4. Разрешь доступ
5. Готово! Ты в Dashboard!

---

## 🐛 Если что-то не работает:

### Docker не запускается
```bash
# Останови все
docker-compose down

# Удали старые контейнеры
docker-compose down -v

# Пересобери
docker-compose up -d --build
```

### "Invalid redirect URI"
Проверь что в Google Cloud Console добавлен ТОЧНО такой URI:
```
http://localhost:8004/auth/google/callback
```

### Показывает старый фронт
```bash
# Жесткая перезагрузка браузера
Ctrl + Shift + R
```

### Проверка что все работает
```bash
# Auth service
curl http://localhost:8004/health

# IoT service
curl http://localhost:8001/health

# PostgreSQL
docker exec -it eco_postgres psql -U eco_monitor -d environmental_monitoring -c "SELECT version();"
```

---

## 📞 Нужна помощь?

### Посмотри логи:
```bash
# Все сервисы
docker-compose logs -f

# Только auth
docker-compose logs -f auth_service

# Только frontend
docker-compose logs -f frontend
```

### Проверь что в базе:
```bash
docker exec -it eco_postgres psql -U eco_monitor -d environmental_monitoring

# Внутри:
\dt                  # Все таблицы
SELECT * FROM users; # Пользователи
\q                   # Выход
```

---

## ✅ КОРОТКО:

1. `cd frontend && npm install` - убрать ошибки VS Code
2. Не качай PostgreSQL - он в Docker
3. Не регайся в Firebase - он не нужен
4. Зарегайся в Google Cloud Console (для OAuth)
5. Создай `.env` с Google credentials
6. Запусти `docker-compose up -d`
7. Открой http://localhost:3000
8. Войди через Google

Все!
