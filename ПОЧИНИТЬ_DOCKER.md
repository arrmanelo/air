# 🔧 КАК ПОЧИНИТЬ DOCKER (твоя ошибка)

## Проблема:
```
failed to prepare extraction snapshot: parent snapshot does not exist: not found
```

Это значит что Docker кэш поломался при сборке.

## Решение (5 минут):

### Шаг 1: Останови и удали ВСЕ контейнеры
```bash
docker-compose down -v
```

### Шаг 2: Удали все образы проекта
```bash
docker rmi -f $(docker images | grep weimea | awk '{print $3}')
```

Если команда не работает (Windows PowerShell), сделай:
```bash
docker images
```

И удали каждый образ с `weimea`:
```bash
docker rmi -f <IMAGE_ID>
```

### Шаг 3: Очисти Docker кэш полностью
```bash
docker builder prune -a -f
```

### Шаг 4: Пересобери с нуля
```bash
docker-compose build --no-cache
```

### Шаг 5: Запусти все
```bash
docker-compose up -d
```

### Шаг 6: Проверь что все запустилось
```bash
docker-compose ps
```

Все сервисы должны быть в статусе "Up".

---

## Если и это не помогло:

### Вариант 2: Полная очистка Docker (жесткий метод)

⚠️ **ВНИМАНИЕ:** Это удалит ВСЕ Docker данные (не только этого проекта)!

```bash
# Останови все контейнеры
docker stop $(docker ps -aq)

# Удали все контейнеры
docker rm $(docker ps -aq)

# Удали все образы
docker rmi $(docker images -q)

# Удали все volumes
docker volume prune -f

# Удали все build cache
docker builder prune -a -f

# Теперь собери проект
cd C:\Users\agark\Desktop\Новая папка\weimea
docker-compose up -d --build
```

---

## После успешного запуска:

Проверь что все работает:

```bash
# Все сервисы должны быть "Up"
docker-compose ps

# Проверь логи
docker-compose logs -f

# Проверь auth service
curl http://localhost:8004/health

# Открой браузер
http://localhost:3000
```

---

## Почему это случилось?

Docker пытался использовать старый кэш который был создан с Firebase зависимостями. После того как мы убрали Firebase из `nuxt.config.ts`, кэш стал невалидным.

Полная пересборка с `--no-cache` решит эту проблему.

---

## Твой .env правильный! ✅

```bash
GOOGLE_CLIENT_ID=830876898-i9qdju5u71r56e8ghakhei2aiol8sj18.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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
JWT_SECRET_KEY=my-super-secret-key-change-in-production-12345
```

Теперь можешь **УДАЛИТЬ** эту строку из `.env` (она больше не нужна):
```bash
FIREBASE_PROJECT_ID=your-project-id
```

---

## Быстрая команда (копируй всю):

```bash
cd "C:\Users\agark\Desktop\Новая папка\weimea" && docker-compose down -v && docker builder prune -a -f && docker-compose build --no-cache && docker-compose up -d
```

Подожди 3-5 минут пока все соберется и запустится.

Затем открой: http://localhost:3000

---

## Ошибки в VS Code про `defineNuxtConfig` исчезнут после:

```bash
cd frontend
npm install
```

Это установит все зависимости локально и TypeScript будет их видеть.

---

## Итого:

1. ✅ Убрали Firebase из кода (я уже сделал)
2. ✅ Обновили .env.example
3. ⏳ Тебе нужно: очистить Docker и пересобрать
4. ⏳ Запустить `npm install` в папке frontend
5. ✅ Открыть http://localhost:3000 и войти через Google

Все готово к работе! 🚀
