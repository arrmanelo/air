# 🔍 ДИАГНОСТИКА ПРОБЛЕМЫ

## Шаг 1: Проверь статус Docker Desktop

### Windows:
Посмотри в трей (правый нижний угол) - Docker Desktop должен быть запущен (иконка кита).

Если не запущен:
1. Запусти Docker Desktop
2. Подожди пока он полностью загрузится (иконка перестанет мигать)
3. Попробуй снова

---

## Шаг 2: Проверь что контейнеры запустились

Открой PowerShell/CMD в папке проекта:
```bash
cd "C:\Users\agark\Desktop\Новая папка\weimea"
docker-compose ps
```

**Что должно быть:**
```
NAME                      STATUS
eco_postgres              Up
eco_auth_service          Up
eco_iot_service           Up
eco_analytics_service     Up
eco_alert_service         Up
eco_frontend              Up
```

**Если все "Up"** - переходи к Шагу 3.

**Если есть "Exit" или "Restarting"** - переходи к Шагу 4.

**Если пусто (ничего нет)** - контейнеры не создались, смотри Шаг 5.

---

## Шаг 3: Если все "Up" но браузер не открывается

Проверь занятость портов:

```bash
netstat -ano | findstr :3000
netstat -ano | findstr :8004
```

**Если порты заняты:**

Найди процесс который занял порт:
```bash
# Смотри последнее число (PID) в выводе netstat
# Например: TCP 0.0.0.0:3000 ... LISTENING 12345
# PID = 12345

# Убей процесс:
taskkill /PID 12345 /F
```

Или измени порты в `docker-compose.yml`:
```yaml
# Для frontend измени:
ports:
  - "3001:3000"  # Теперь будет на localhost:3001
```

---

## Шаг 4: Если контейнеры в статусе "Exit" или "Restarting"

Смотри логи проблемного сервиса:

```bash
# Посмотри какой сервис не запустился
docker-compose ps

# Смотри логи этого сервиса (например auth_service)
docker-compose logs auth_service

# Или все логи сразу
docker-compose logs
```

**Скинь мне логи** - я помогу разобраться!

---

## Шаг 5: Если контейнеры вообще не создались

### Вариант А: Ошибка при сборке

Запусти с выводом ошибок:
```bash
docker-compose up --build
```

Посмотри где ошибка. Частые проблемы:

**"Cannot connect to Docker daemon"**
- Решение: Запусти Docker Desktop

**"Port is already allocated"**
- Решение: Измени порт в docker-compose.yml или убей процесс на этом порту

**"No space left on device"**
- Решение: Очисти Docker: `docker system prune -a`

### Вариант Б: Файл docker-compose.yml не найден

Убедись что ты в правильной папке:
```bash
cd "C:\Users\agark\Desktop\Новая папка\weimea"
dir docker-compose.yml
```

Должен показать файл. Если нет - ты не в той папке.

---

## Шаг 6: Проблема с интернетом

**Влияет ли интернет?**

### На локальный запуск: ❌ НЕТ
Docker контейнеры запускаются локально, интернет НЕ нужен.

### На первую сборку: ✅ ДА
Если ты первый раз запускаешь, Docker скачивает образы:
- `python:3.11-slim`
- `node:20-slim`
- `timescale/timescaledb:latest-pg15`

Без интернета скачать не сможет.

**Решение:**
1. Проверь интернет: `ping google.com`
2. Если интернета нет - подожди пока появится
3. Если интернет есть но Docker не качает - проверь настройки прокси в Docker Desktop

### На вход через Google OAuth: ✅ ДА
Для входа через Google нужен интернет! Без интернета OAuth не работает.

---

## Шаг 7: Быстрая диагностика (копируй по порядку)

```bash
# 1. Проверь Docker запущен
docker --version

# 2. Проверь контейнеры
docker-compose ps

# 3. Проверь логи
docker-compose logs --tail=50

# 4. Проверь порты
netstat -ano | findstr :3000
netstat -ano | findstr :8004

# 5. Проверь интернет
ping google.com
```

---

## 📸 Что мне скинуть для помощи?

Скинь мне вывод этих команд:

```bash
docker-compose ps
docker-compose logs --tail=100
```

Или скриншот Docker Desktop (где показаны контейнеры).

---

## 🔥 Если ничего не помогает - ПОЛНЫЙ СБРОС

```bash
# 1. Останови все
docker-compose down -v

# 2. Удали все образы проекта
docker images | findstr weimea
# Запомни IMAGE ID каждого
docker rmi -f IMAGE_ID_1 IMAGE_ID_2 ...

# 3. Очисти кэш
docker system prune -a -f

# 4. Пересобери с нуля (НУЖЕН ИНТЕРНЕТ!)
docker-compose build --no-cache

# 5. Запусти
docker-compose up -d

# 6. Смотри логи
docker-compose logs -f
```

---

## ✅ Что должно работать после успешного запуска:

```bash
# Проверь контейнеры
docker-compose ps
# Все должны быть "Up"

# Проверь порты
curl http://localhost:8004/health
# Ответ: {"status":"healthy"}

curl http://localhost:3000
# Должен вернуть HTML фронтенда
```

Открой в браузере:
- http://localhost:3000 - фронтенд
- http://localhost:8004 - auth API
- http://localhost:8001 - IoT API

---

## 💡 Частые причины:

1. **Docker Desktop не запущен** ← Самая частая причина!
2. **Порты заняты другими программами**
3. **Нет интернета при первой сборке**
4. **Docker не хватает памяти** (увеличь в настройках Docker Desktop: Settings → Resources → Memory → поставь минимум 4GB)
5. **Антивирус блокирует Docker**

---

## 🆘 Скинь мне:

```bash
docker-compose ps
docker-compose logs auth_service --tail=50
docker-compose logs frontend --tail=50
```

И я точно скажу в чем проблема!
