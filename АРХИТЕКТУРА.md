# 🎯 НАША АРХИТЕКТУРА

## Что у нас РЕАЛЬНО используется сейчас:

```
┌─────────────────────────────────────────┐
│  Frontend (Nuxt 3)                      │
│  http://localhost:3000 (Docker)         │
│  http://localhost:3001 (npm run dev)    │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│  Auth Service (Google OAuth + JWT)      │
│  http://localhost:8004                  │
└─────────────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│  PostgreSQL (БЕЗ установки!)            │
│  localhost:5432 (только через Docker)   │
│  Данные: users, sensors, alerts         │
└─────────────────────────────────────────┘
```

## ❌ Что НЕ НУЖНО:

- ❌ Firebase (убрали, используем Google OAuth напрямую)
- ❌ Устанавливать PostgreSQL локально
- ❌ Устанавливать pgAdmin
- ❌ Регистрироваться в Firebase Console

## ✅ Что НУЖНО сделать:

1. ✅ Зарегистрироваться в Google Cloud Console (для OAuth)
2. ✅ Создать `.env` файл с GOOGLE_CLIENT_ID и GOOGLE_CLIENT_SECRET
3. ✅ Запустить `docker-compose up -d`
4. ✅ Открыть http://localhost:3000

## 🔧 Как запустить ВСЕ ВМЕСТЕ:

### Вариант 1: Docker (рекомендуется - все работает)

```bash
# Остановите все старое
docker-compose down

# Запустите все сервисы (включая PostgreSQL!)
docker-compose up -d --build

# Проверьте что все запустилось
docker-compose ps
```

**Порты:**
- Frontend: http://localhost:3000 ✅ (новый фронт + бэк)
- Auth API: http://localhost:8004
- IoT API: http://localhost:8001
- Analytics API: http://localhost:8002
- Alert API: http://localhost:8003
- PostgreSQL: localhost:5432 (внутри Docker)

### Вариант 2: npm run dev (для разработки фронтенда)

```bash
# В папке frontend
npm install
npm run dev
```

Откроется на http://localhost:3001

**НО:** Бэкенд все равно должен быть запущен через Docker!

```bash
# В корне проекта
docker-compose up -d postgres auth_service iot_service analytics_service alert_service
```

## 🐘 PostgreSQL - БЕЗ установки!

PostgreSQL запускается **автоматически** в Docker контейнере!

### Как посмотреть данные в базе?

```bash
# Войти в PostgreSQL через Docker
docker exec -it eco_postgres psql -U eco_monitor -d environmental_monitoring

# Внутри PostgreSQL:
\dt              # Показать все таблицы
SELECT * FROM users;        # Посмотреть пользователей
SELECT * FROM sensors;      # Посмотреть сенсоры
SELECT * FROM alerts;       # Посмотреть алерты
\q               # Выйти
```

### Или используйте любой GUI клиент (опционально):

**DBeaver** (бесплатный):
- Host: localhost
- Port: 5432
- Database: environmental_monitoring
- User: eco_monitor
- Password: eco_pass

## 🔥 Firebase - больше НЕ ИСПОЛЬЗУЕТСЯ!

Мы **убрали Firebase** и заменили на:
- **Google OAuth** (напрямую через Auth Service)
- **PostgreSQL** (вместо Firestore)
- **JWT токены** (вместо Firebase Auth)

Переменные `NUXT_PUBLIC_FIREBASE_*` можно удалить - они не нужны!

## 🐛 Ошибки в VS Code - это НОРМАЛЬНО!

Ошибки типа:
```
Cannot find name 'defineNuxtConfig'
Cannot find name 'process'
```

Это потому что локально не установлены node_modules!

**Решение:**
```bash
cd frontend
npm install
```

После этого ошибки исчезнут.

## 🚀 Полная инструкция запуска:

### 1. Установите зависимости фронтенда (один раз)
```bash
cd frontend
npm install
cd ..
```

### 2. Создайте .env файл
```bash
cp .env.example .env
nano .env
```

Добавьте:
```bash
GOOGLE_CLIENT_ID=ваш-client-id-от-google
GOOGLE_CLIENT_SECRET=ваш-client-secret-от-google
JWT_SECRET_KEY=любая-случайная-строка-32-символа
```

### 3. Запустите Docker
```bash
docker-compose up -d --build
```

### 4. Откройте браузер
http://localhost:3000

### 5. Войдите через Google
Нажмите "Continue with Google" на странице логина

## 🎯 Что происходит при входе:

1. Нажимаете "Continue with Google"
2. Перенаправляет на Google (БЕЗ Firebase!)
3. Google возвращает на http://localhost:8004/auth/google/callback
4. Auth Service:
   - Получает ваши данные от Google
   - Сохраняет в **PostgreSQL** (таблица users)
   - Генерирует JWT токен
   - Перенаправляет на http://localhost:3000/auth/callback
5. Frontend сохраняет токен и открывает Dashboard

## 📱 Порты и где что:

| Сервис | Docker | npm run dev | Назначение |
|--------|--------|-------------|------------|
| Frontend | 3000 | 3001 | Интерфейс |
| Auth Service | 8004 | - | Google OAuth + JWT |
| IoT Service | 8001 | - | Данные сенсоров |
| Analytics | 8002 | - | AI анализ |
| Alert Service | 8003 | - | Оповещения |
| PostgreSQL | 5432 | - | База данных |

## ❓ Частые вопросы:

### Q: Нужно ли качать PostgreSQL?
**A:** НЕТ! Он в Docker контейнере.

### Q: Нужно ли регистрироваться в Firebase?
**A:** НЕТ! Мы больше не используем Firebase.

### Q: Почему localhost:3000 показывает старый фронт?
**A:** Либо не перезапустили Docker, либо кэш браузера. Сделайте:
```bash
docker-compose down
docker-compose up -d --build
# Ctrl+Shift+R в браузере (жесткая перезагрузка)
```

### Q: Почему на localhost:3001 нет бэкенда?
**A:** `npm run dev` запускает только фронтенд! Бэк должен быть в Docker:
```bash
docker-compose up -d
```

### Q: Как убрать ошибки в VS Code?
**A:** Установите зависимости:
```bash
cd frontend
npm install
```

## 🎓 Итого:

1. **Установите Google OAuth** (см. ИНСТРУКЦИЯ.md)
2. **Создайте .env** с Google credentials
3. **Запустите Docker**: `docker-compose up -d`
4. **Откройте**: http://localhost:3000
5. **Войдите** через Google

PostgreSQL и все бэкенды запустятся автоматически в Docker!
