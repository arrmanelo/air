# 🚀 Инструкция по запуску с Google OAuth

## 📝 Что нужно сделать

### 1. Получить Google OAuth credentials

#### Шаг 1: Зайти в Google Cloud Console
Откройте: https://console.cloud.google.com/

#### Шаг 2: Создать новый проект
1. Нажмите на выпадающий список проектов вверху
2. Нажмите "New Project" (Новый проект)
3. Название: например "Weimea" или "EcoMonitor"
4. Нажмите "Create" (Создать)

#### Шаг 3: Настроить OAuth Consent Screen
1. В левом меню найдите "APIs & Services" → "OAuth consent screen"
2. Выберите "External" (Внешние пользователи)
3. Нажмите "Create" (Создать)
4. Заполните:
   - App name: `Weimea EcoMonitor`
   - User support email: ваш email
   - Developer contact: ваш email
5. Нажмите "Save and Continue" (Сохранить и продолжить)
6. На странице "Scopes" просто нажмите "Save and Continue"
7. На странице "Test users":
   - Нажмите "+ ADD USERS"
   - Добавьте свой Google email (с которого будете входить)
   - Нажмите "Add" и "Save and Continue"

#### Шаг 4: Создать OAuth Client ID
1. В левом меню: "APIs & Services" → "Credentials"
2. Нажмите "Create Credentials" → "OAuth client ID"
3. Application type: выберите **"Web application"**
4. Name: `Weimea Auth`
5. В разделе "Authorized redirect URIs" нажмите "+ ADD URI" и добавьте:
   ```
   http://localhost:8004/auth/google/callback
   ```
6. Нажмите "Create" (Создать)
7. **ВАЖНО:** Появится окно с Client ID и Client Secret - **СКОПИРУЙТЕ ИХ!**
   - Client ID выглядит как: `123456789-abcdef.apps.googleusercontent.com`
   - Client Secret выглядит как: `GOCSPX-abcdefghijk123456`

### 2. Настроить .env файл

Создайте файл `.env` в корне проекта:

```bash
# Скопируйте из примера
cp .env.example .env
```

Откройте файл `.env` и вставьте ваши данные:

```bash
# Google OAuth (ОБЯЗАТЕЛЬНО!)
GOOGLE_CLIENT_ID=вставьте-сюда-ваш-client-id
GOOGLE_CLIENT_SECRET=вставьте-сюда-ваш-client-secret

# JWT Secret (можете оставить как есть для локальной разработки)
JWT_SECRET_KEY=my-super-secret-key-change-in-production-12345

# PostgreSQL (можете не менять)
POSTGRES_USER=eco_monitor
POSTGRES_PASSWORD=eco_pass
POSTGRES_DB=environmental_monitoring

# Gemini API (если есть, если нет - оставьте пустым)
GEMINI_API_KEY=ваш-ключ-или-оставьте-пустым
```

### 3. Запустить приложение

```bash
# Запустить все сервисы
docker-compose up -d

# Посмотреть логи (чтобы убедиться что все запустилось)
docker-compose logs -f
```

Дождитесь пока все сервисы запустятся (30-60 секунд).

### 4. Открыть приложение

Откройте в браузере: http://localhost:3000

### 5. Войти через Google

1. Нажмите кнопку "Login" или "Get Started"
2. Нажмите "Continue with Google"
3. Выберите ваш Google аккаунт (тот который добавили как Test user)
4. Разрешите доступ к вашему профилю
5. Вас автоматически перенаправит в Dashboard!

## ✅ Готово!

После входа:
- Ваш email и имя сохранятся в базе данных PostgreSQL
- Вы получите JWT токен на 7 дней
- Токен хранится в localStorage браузера

## 🔍 Проверка что все работает

### Проверить что auth_service запустился:
```bash
curl http://localhost:8004/health
```

Должен вернуть: `{"status":"healthy"}`

### Проверить базу данных:
```bash
docker exec -it eco_postgres psql -U eco_monitor -d environmental_monitoring -c "SELECT * FROM users;"
```

После входа через Google увидите вашу запись в таблице!

## ❌ Возможные проблемы

### "Invalid redirect URI" при входе
**Решение:** Убедитесь что в Google Cloud Console добавили ТОЧНО такой URI:
```
http://localhost:8004/auth/google/callback
```

### "Access blocked: Authorization Error"
**Решение:** Добавьте себя как Test User в OAuth consent screen (Шаг 3.7 выше)

### Сервисы не запускаются
**Решение:**
```bash
# Остановите все
docker-compose down

# Удалите старые контейнеры
docker-compose down -v

# Запустите заново
docker-compose up -d --build
```

### "Client ID not set" в логах
**Решение:** Проверьте что файл `.env` создан и содержит `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET`

## 📱 Порты сервисов

- Frontend: http://localhost:3000
- Auth API: http://localhost:8004
- IoT API: http://localhost:8001
- Analytics API: http://localhost:8002
- Alert API: http://localhost:8003
- PostgreSQL: localhost:5432

## 🎯 Что происходит при входе?

1. Вы нажимаете "Continue with Google"
2. Перенаправляет на Google OAuth (accounts.google.com)
3. Вы входите через Google
4. Google перенаправляет на `http://localhost:8004/auth/google/callback`
5. Auth service:
   - Получает ваши данные от Google
   - Проверяет есть ли вы в базе
   - Если нет - создает нового пользователя
   - Генерирует JWT токен
   - Перенаправляет на `http://localhost:3000/auth/callback?token=...`
6. Frontend:
   - Сохраняет токен в localStorage
   - Загружает ваши данные
   - Перенаправляет в Dashboard

## 🔐 Безопасность

- ✅ Пароли не хранятся (используется Google OAuth)
- ✅ JWT токены с истечением через 7 дней
- ✅ Данные хранятся в PostgreSQL
- ✅ CORS настроен только для localhost

## 💾 Где хранятся данные?

- **Пользователи:** PostgreSQL в таблице `users`
- **Сенсоры:** PostgreSQL в таблице `sensor_readings`
- **Алерты:** PostgreSQL в таблице `alerts`

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи: `docker-compose logs -f auth_service`
2. Проверьте что все сервисы запущены: `docker-compose ps`
3. Убедитесь что `.env` файл заполнен правильно

---

**Важно:** Файл `.env` НЕ коммитится в git! Храните ваши credentials в безопасности.
